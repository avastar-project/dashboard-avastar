import { Link } from 'react-router-dom';

export default function Google() {
  return (
    <div>
      <h1>Google</h1>
      <nav>
        <Link to="/">back to homepage</Link>
      </nav>
    </div>
  );
}
